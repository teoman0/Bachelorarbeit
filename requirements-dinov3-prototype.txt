# Minimal pinned environment used for the DINOv3 prototype heatmap runs.
# CPU-only PyTorch wheels are used here for reproducibility on the tested machine.
# CUDA users may replace the PyTorch index and torch/torchvision builds.

--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.4.6
Pillow==12.2.0
PyYAML==6.0.3
torch==2.12.1+cpu
torchvision==0.27.1+cpu
transformers==5.12.1
